## What happened

<!-- Observed behavior, ideally with the exact error/output. -->

## What was expected instead

## How it was found

<!-- Real repro steps preferred over a hypothesis. If found via a real
     system (CI, a live run, production), say so explicitly — that's
     stronger evidence than a code read-through. -->

## Root cause (fill in once known)

## Fix verification

- [ ] The specific failure no longer reproduces
- [ ] Full test suite green
- [ ] If this was a trigger→consequence bug (a thing that was supposed
      to happen silently didn't), the consequence is now independently
      verified as happening, not just the trigger reporting success
- [ ] Regression test added covering this specific case
